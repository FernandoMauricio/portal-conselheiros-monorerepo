port: 7880
rtc:
  udp_port: 7882
  tcp_port: 7881
  # ICE servers (STUN/TURN)
  # configure this to your coturn server or use a public one
  # urls: 
  #   - stun:stun.l.google.com:19302
  #   - turn:your-turn-server.com:3478
  # username: your-turn-username
  # credential: your-turn-password

# API keys
api_keys:
  - key: "${LIVEKIT_API_KEY}"
    secret: "${LIVEKIT_API_SECRET}"

# Redis for clustering (optional)
# redis:
#   address: "redis:6379"

# Webhook callbacks
# webhooks:
#   - url: "http://localhost:3002/api/webhooks"

# Egress (recording) configuration
# egress:
#   file_path: "/tmp/recordings"
#${room_name}.mp4"
#   s3:
#     access_key: "${MINIO_ROOT_USER}"
#     secret_key: "${MINIO_ROOT_PASSWORD}"
#     endpoint: "http://minio:9000"
#     bucket: "portal-conselheiros"
#     region: "us-east-1"


